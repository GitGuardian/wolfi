name: redis-bitnami

on:
  schedule:
    - cron: "00 01 * * 1-5"
  pull_request:
    paths:
      - .github/workflows/redis-bitnami.yaml
      - 'images/redis-bitnami/*.yaml'
      - 'images/redis-bitnami/**/*.yaml'
  push:
    branches:
      - 'main'
    paths:
      - .github/workflows/redis-bitnami.yaml
      - 'images/redis-bitnami/*.yaml'
      - 'images/redis-bitnami/**/*.yaml'
  workflow_dispatch:

permissions:
  contents: read
  packages: write
  attestations: write
  id-token: write
  security-events: write
  actions: read

jobs:
  publish:
    strategy:
      matrix:
        version: [latest, "7.2"]
        variant: [prod]
    name: ${{ matrix.version }}${{ matrix.variant == 'shell' && '-shell' || matrix.variant == 'dev' && '-dev' || '' }}
    uses: './.github/workflows/release.yaml'
    with:
      tag: ${{ matrix.version }}${{ matrix.variant == 'shell' && '-shell' || matrix.variant == 'dev' && '-dev' || '' }}
      target: ${{ format('{0}/{1}', matrix.version, matrix.variant) }}
    secrets: inherit
